# 環境構築

## tmuxのインストール
mac
```
brew install tmux
```

## zshのインストール
mac
```
brew install zsh
sudo vi /etc/shells
# /usr/local/bin/zshを追記
chsh -s /usr/local/bin/zsh
```

## npmコマンド補完
npmインストール後に
```
npm completion >> ~/.zshrc
```

